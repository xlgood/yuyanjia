# 云开发控制台总清单（一次性收口）

> 用途：把云开发控制台侧所有需要人工操作的项汇总成一张打勾表，做完即不会再漏。
> 环境 ID：`cloud1-d0gyxil2hba0873d3`｜AppID：`wx72441ae31dfd9b5a`
> 更新时间：2026-08-12（状态为当前实际进度）

---

## A. 集合与权限（12 个）

- [x] 已创建：`users` `markets` `bets` `pks` `arbitrations` `arbitration_votes` `invites` `rank_snapshots` `leaderboards` `data_sources` `resolution_logs`
- [ ] `ad_rewards`（广告接入时创建即可，规则同下）
- [x] 全部集合权限 = 「所有用户不可读写」（前端零直连数据库，验证脚本见 `database/security-rules.md`）

## B. 索引（21 个，权威清单见 `database/indexes.md`）

- [x] markets × 3：`idx_status_deadline`、`idx_category_status_deadline`、`idx_status_totalPool`（totalPool **降序**、全部非唯一）
- [x] users × 4：`idx_streak` / `idx_weekPoints` / `idx_monthPoints` / `idx_totalPoints`（均降序）
- [x] bets × 2：`idx_openid_createdAt`（createdAt 降序）、`idx_marketId_status`
- [x] pks × 6：`idx_status_expiresAt`、`idx_opponentId_status_createdAt`、`idx_challengerId_createdAt`、`idx_opponentId_createdAt`、`idx_status_participantIds`、`idx_marketId_participantIds_status`
- [x] invites × 2：`idx_inviterId_createdAt`、`idx_inviterId_inviterRewarded`
- [x] rank_snapshots × 1：`idx_type_date`（date 降序）
- [x] arbitrations × 3~4：`idx_marketId_status`、`idx_marketId_createdAt`、`idx_status_endsAt`（+可选 `idx_challengerOpenid_createdAt`）
- [x] arbitration_votes × 2：`idx_arbitrationId`、`idx_openid`
- 自检要点：**全部非唯一**；`totalPool`/`createdAt`/`date` 等排序字段方向与清单一致

## C. 云函数部署（37 + _shared）

- [x] 已部署：login / checkHonors / adRewardCallback / settleMarket / settleArbitration / resolver / getLeaderboard / pkLeaderboard / rankSnapshot / getMarkets / createMarket / placeBet / createPk / respondPk / myPks / migratePoints / checkIn / claimAdTask / claimRelief / createArbitration / voteArbitration（均已直传成功）
- [ ] 其余未改动的函数确认已在云端（首次 `npm run deploy:all` 可全量兜底，自动跳过 `_shared`）
- 部署方式：`npm run deploy`（脚本）或开发者工具 CLI（`--paths` 模式）；**CLI 上传不会同步 config.json 触发器**，见 E

## D. 环境变量（云函数 → 配置）

| 变量 | 配置位置 | 状态 |
|------|----------|------|
| `ADMIN_OPENIDS` | 11 个管理函数（createMarket/upsertDataSource/getPendingReviews/getDashboardStats/aiDraftSpec/aiSuggestTopics/resolveMarket/settleMarket/rankSnapshot/resetPeriods/migratePoints 等） | [ ] |
| `AI_PROVIDER` | aiSuggestTopics：**必须为 `deepseek`**（曾误配 `custom` 网关 + kimi-k3 导致联网失效；代码默认即 deepseek） | [ ] |
| `DEEPSEEK_API_KEY` | aiDraftSpec / aiSuggestTopics（曾因复制损坏需重新填写） | [ ] |
| `CUSTOM_API_KEY` / `CUSTOM_BASE_URL` / `CUSTOM_MODEL` / `CUSTOM_SEARCH` | aiSuggestTopics（路线一不需要，建议删除，避免误走网关） | [ ] |
| `DISPUTE_WINDOW_HOURS`（可选，默认 5） | resolveMarket / resolver | [ ] |
| `LOCK_WEBHOOK_URL` / `LOCK_WEBHOOK_TYPE`（可选） | lockMarkets | [ ] |
| `ALERT_WEBHOOK_URL`（可选，默认回退 LOCK_WEBHOOK_URL） | settleMarket / settleArbitration / resolver | [ ] |
| `HONORS_CHECK_INTERVAL_MINUTES`（可选，默认 10） | login | [ ] |
| `LEADERBOARD_CACHE_MINUTES`（可选，默认 10） | getLeaderboard / pkLeaderboard | [ ] |
| `INVITE_INVITEE_POINTS` / `INVITE_INVITER_POINTS` / `INVITE_DAILY_CAP`（可选） | login / placeBet | [ ] |
| `SUBSCRIBE_JUDGE_TMPL` / `SUBSCRIBE_ARBITRATION_TMPL` | settleMarket / settleArbitration（+前端 config.js） | [ ] |
| `AD_SSV_TOKEN` / `AD_SSV_ENCODING_AES_KEY` / `AD_SSV_REWARD_ITEM` + `AD_SSV_ENABLED=true` | adRewardCallback / claimAdTask（广告接入时） | [ ] |

## E. 定时触发器（8 个，控制台「触发管理」手工建）

- [x] lockMarkets：`0 * * * * * *`（每分钟）
- [x] resolver：`0 */10 * * * * *`
- [x] settleMarket：`0 */10 * * * * *`
- [x] settleArbitration：`0 */5 * * * * *`
- [x] myPks：`0 */10 * * * * *`
- [x] rankSnapshot：`0 55 23 * * * *`（每天 23:55）
- [x] resetPeriods：`0 0 0 * * MON *`（周一）+ `0 0 0 1 * * *`（每月 1 日）
- 验收：`lockMarkets` 调用日志每分钟一条；其余按周期有记录

## F. 数据迁移

- [ ] **重跑 `migratePoints`**（云函数 → 云端测试，入参留空）——回填 `totalPool` 后返回含 `"marketTotalPoolBackfilled": N`（N>0 表示本次补齐；0 表示已回填过）。跑完首页「热门」标签应正常
- [x] 已跑过：榜单净收益重算 + inviteCount 回填（首次）

## G. 上线前总验收

- [ ] 真机全流程回归（iOS + Android，见 `docs/提审准备.md` 第二节清单）
- [ ] 触发器日志抽查：lockMarkets 每分钟、resolver/settleMarket 每 10 分钟有执行记录
- [ ] 客户端直读数据库被拒（安全规则验证脚本）
- [ ] 首页「热门」标签正常（依赖 B 的 idx_status_totalPool + F 的 totalPool 回填）
- [ ] git 推送到 GitHub 远程，Actions 的 CI 全绿
